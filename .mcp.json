{
  "name": "translator-pro-ai-mcp",
  "description": "AI-powered translator pro ai MCP server for agents. Supports translate text, detect language, compare translations. By MEOK AI Labs.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "translate_text",
      "description": "Translate text using built-in phrase dictionary. Word-by-word with phrase matching.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "target_language": {
            "type": "string"
          },
          "source_language": {
            "type": "string"
          }
        },
        "required": [
          "text",
          "target_language"
        ]
      }
    },
    {
      "name": "detect_language",
      "description": "Detect the language of input text using word frequency and character patterns.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ]
      }
    },
    {
      "name": "compare_translations",
      "description": "Compare translations of the same text across multiple target languages.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "languages": {
            "type": "array"
          },
          "source_language": {
            "type": "string"
          }
        },
        "required": [
          "text",
          "languages"
        ]
      }
    },
    {
      "name": "get_supported_languages",
      "description": "List all supported languages with their codes and capabilities.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  ]
}